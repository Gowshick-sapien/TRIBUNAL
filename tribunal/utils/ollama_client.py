"""Ollama inference wrapper — stub."""

# Implementation: Stage 1 — query parsing and defense agent only


def generate(prompt: str, model: str | None = None) -> str:
    """Call Ollama and return model response text."""
    ...
